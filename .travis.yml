sudo: false
language: cpp
compiler: gcc
dist: trusty

Variables
env:
    #- DOXYFILE=doc/Doxyfile
    - PATH

script:
    - make
    - make test
    - test/Main_TestAll

addons:
    sources:
        - boost-latest
    apt:
        packages:
            - libboost1.55-all-dev
              #- cmake
              #- doxygen

script:
    - cmake CMakeLists.txt && make
    - test/testAll

#after_success: doxygen
